// Wait for DOM to load
document.addEventListener('DOMContentLoaded', () => {
    console.log('Portfolio initialized successfully.');
    
    // Future scripts for scroll animations, marquee control, etc.
});
